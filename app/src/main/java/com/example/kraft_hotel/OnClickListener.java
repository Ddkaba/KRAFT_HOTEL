package com.example.kraft_hotel;

import java.util.ArrayList;

public interface OnClickListener {
    void OnClicked(String full_description, ArrayList<Integer>Photo);
    void OnClicked(String url);
}
